# This file defines notification recipients. IDs select recipients and the
# isAdmin flag controls error notifications.
---
recipient1:
  # If isAdmin is set to true, this recipient will be sent an alert if
  # there have been any errors in the last hour.
  isAdmin: true
recipient2: {}
